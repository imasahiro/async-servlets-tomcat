# async-servlets-tomcat
An example with Tomcat 8 and Servlets with asynchronous support

# available servlet calls
curl http://localhost:8080/sync

curl http://localhost:8080/async

curl http://localhost:8080/reactive
